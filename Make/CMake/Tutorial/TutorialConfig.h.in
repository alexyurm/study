//the configured options and settings for Tutorial
/*
* Since the configured file will be written into the binary tree(??) we must add that 
* directory to the lists of paths to search for include files We then create a TutorialConfig.h.in
* file in the source tree with the following contents:        
* 
*/
#define Tutorial_VERSION_MAJOR @Tutorial_VERSION_MAJOR@
#define Tutorial_VERSION_MINOR @Tutorial_VERSION_MINOR@

#cmakedefine USE_MYMATH
