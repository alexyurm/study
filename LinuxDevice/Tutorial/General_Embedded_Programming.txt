1. A compiler tool chain is a collection of programs that is used for compiling and building programs. It consists of three parts: 1) the compiler; 2) binary utilities(binutils); 3) standard libraries. For example, the compiler, linker, and assembler are parts of the compiler toolchain.

2. 
