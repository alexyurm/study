/*
*
* Given a set of number ranges, i.e. [1,5], [3,6], [7,9], write a program to merge overlapping number ranges. In above example, the result should be [1,6], [7,9].
*
*/

class Range {
   public int lower;
   public int upper;
   
   // Merge two ranges into one
   public static Range merge(Range x, Range y) {
      if 
      
      if (y.lower < x.lower) {
         if (y.upper > x.upper) {
            
         } else if (y.upper < x.upper && y.upper > x.lower) {
            
         }
      }
   }
}

class problem3 {
   
}
